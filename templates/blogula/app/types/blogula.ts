export interface BlogulaSocial {
  label: string;
  to: string;
  icon: string;
}

export interface BlogulaEvent {
  date: string;
  title: string;
  icon: string;
  description: string;
  link?: string;
}
